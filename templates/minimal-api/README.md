# Minimal API Template

This template provides a lightweight REST API boilerplate pre-configured with the ONBP Core Framework (Kernel, Dependency Container, Logger, Health Checks).

## Quick Start
```bash
pnpm install
pnpm dev
```
