# {{pascalName}} Module

Scaffolded automatically by the ONBP CLI.

## Description
This module provides the business logic, storage, and endpoints for managing `{{normalizedName}}`.

## Responsibilities
- Coordinates the service and repository layers for `{{normalizedName}}`.
- Validates requests via Zod.
- Emits events when `{{normalizedName}}` resources change.

## API Endpoints
- `GET /api/v1/{{normalizedName}}` - Retrieve all `{{normalizedName}}` items.
- `POST /api/v1/{{normalizedName}}` - Create a new `{{normalizedName}}` item.

## Events Published
- `{{normalizedName}}.created` - Emitted when a new resource is created.
